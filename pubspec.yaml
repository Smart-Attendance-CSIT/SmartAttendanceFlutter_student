name: smart_attendance_student
description: A college project.

publish_to: "none"

version: 0.0.1+1

environment:
  sdk: ">=2.18.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_barcode_scanner: ^2.0.0
  dio: ^5.0.0

  pretty_dio_logger:

  lottie: ^2.2.0

  table_calendar: ^3.0.9
  shared_preferences: ^2.0.17
  geolocator: ^9.0.2
  geocoding: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/animations/
    - assets/images/google.png
    - assets/images/sadikshya.png

  #   - images/a_dot_ham.jpeg
